/**
 * Wraps the data representing a selectable item in the UI.
 */
// TODO this interface is the same with NameSelectionType in the stars module.
export interface SelectableItem {
  label: string;
  value: number;
}
