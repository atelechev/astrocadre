import { ColorStyle } from '#core/models/theme/color-style';

/**
 * Defines the style properties of the main view background.
 */
export interface BackgroundStyle {
  /**
   * The color to use for the background.
   */
  color: ColorStyle;
}
